import 'package:xayn_discovery_app/presentation/navigation/screen/controller.dart';

/// Serves just to provide an example of the [ScreenController.pop] with result
class ResultModel {
  final bool isCompleted;

  ResultModel(this.isCompleted);
}
